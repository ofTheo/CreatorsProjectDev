<!-- note you need to leave the trailing slash of the local folder -->
<!-- also you need to setup and ssh keys so the machines don't need to use passwords -->
<remote_user>
    theo
</remote_user>
<remote_ip>
    192.168.1.4
</remote_ip>
<remote_osc_port>
    70007
</remote_osc_port>
<command>
    rsync -av -e ssh ! !@!:/Users/!/Desktop/INCOMING_SCANS/
</command>